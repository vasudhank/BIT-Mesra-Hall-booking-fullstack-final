global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: bit-booking-backend
    metrics_path: /api/ops/metrics
    params:
      format:
        - prom
    static_configs:
      - targets:
          - backend:8000
